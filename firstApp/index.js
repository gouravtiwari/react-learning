import React from 'react';
import ReactDOM from 'react-dom';


import App from './components/app.jsx';

// This is only place where we have real DOM to be used

ReactDOM.render(
    <App/>,
    document.getElementById('app')
);
